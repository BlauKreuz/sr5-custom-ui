{{#unless @root.isEditMode}}
    <a data-action="toggleExpanded"
       data-tooltip="{{#if isOpen}}SR5.Collapse{{else}}SR5.Expand{{/if}}"
       data-item-type="{{type}}">
        <i class="{{#if isOpen}}fa-solid fa-angle-up{{else}}fa-solid fa-angle-down{{/if}}"></i>
    </a>
{{/unless}}
